name: 'fakestreamming'

services:
  db:
    image: postgres:18-alpine
    container_name: fakestreamming-db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5432:5432'
    volumes:
      - .data:/var/lib/postgresql18/data
